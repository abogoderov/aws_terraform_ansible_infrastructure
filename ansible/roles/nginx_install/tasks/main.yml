---

  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: present  
    
  - name: Start service nginx
    ansible.builtin.service:
      name: nginx
      state: started

  - name: Enable service nginx
    ansible.builtin.service:
      name: nginx
      enabled: yes


